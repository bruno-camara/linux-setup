#!/bin/bash
# script to configure my Git

GITHUB_USER="bruno-camara"
GITHUB_EMAIL="brunao.camara@gmail.com"

git config --global user.name "$GITHUB_USER"
git config --global user.email "$GITHUB_EMAIL"